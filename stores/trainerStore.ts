export const useTrainerStore = defineStore('trainerStore', () => {
        
        const doSomething = () => {
            return 'soon'
        }

        return {
            doSomething
        }
});