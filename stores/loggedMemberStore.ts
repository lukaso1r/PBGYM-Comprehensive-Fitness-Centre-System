import type {  } from "~/types";

export const useLoggedMemberStore = defineStore('loggedMemberStore', () => {
    
    const doSomething = () => {
        return 'soon'
    }


    return {
        doSomething
    }
});